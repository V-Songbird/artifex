# Artifex — repository map

The map is written once, in [AGENTS.md](AGENTS.md). Read that file: what this
repository is, the two rules that outrank everything, the commands, where each
directory's contents live, the documentation conventions and the pitfalls.

This file exists so a host that looks for `CLAUDE.md` finds it. Keeping the
content in one place is the point — do not copy it here, or the two will drift.
