node_modules/
out/
*.log
.DS_Store

# Agent state: the throwaway worktrees (each a repo of its own, so a `git add -A`
# at the root would stage them as embedded repositories), the local settings and
# the session memory store. None of it is part of the library.
.claude/
